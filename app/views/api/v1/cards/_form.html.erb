<%= form_for(@api_v1_card) do |f| %>
  <% if @api_v1_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_v1_card.errors.count, "error") %> prohibited this api_v1_card from being saved:</h2>

      <ul>
      <% @api_v1_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
